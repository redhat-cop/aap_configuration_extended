---
bugfixes:
  - split pre-commit CI into trusted approval and untrusted test workflows to avoid
    fork pull request checkout failures in pull_request_target
  - restore Ansible collections for external PRs from a default-branch cache populated
    by trusted push and schedule runs
  - skip sanity-py3.12-milestone because ansible-core milestone requires Python >=3.13
