---
bugfixes:
  - filetree_create hub_namespaces export normalizes API empty-string links to an empty list
    before the shared export macros decide whether to omit the field
  - filetree_read sanitizes hub_namespaces links and hub collection remotes or repositories
    empty-string fields so previously exported files import correctly
