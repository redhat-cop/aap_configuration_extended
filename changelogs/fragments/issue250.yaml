---
bugfixes:
  - filetree_create - Export workflow node ``extra_data`` numeric and boolean survey
    values with native YAML types instead of quoted raw strings
    (fixes redhat-cop/aap_configuration_extended#250).
